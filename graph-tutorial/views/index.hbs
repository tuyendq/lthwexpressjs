<div class="jumbotron">
    <h1>{{ title }}</h1>
    <p class="lead">This sample app shows how to user the Microsoft Graph API to access Outlook and OneDrive data from Node.js</p>
    {{#if user}}
        <h4>Welcome {{ user.displayName }}!</h4>
        <p>User the navigation bar at the top of the page to get started.</p>
    {{else}}
        <a href="/auth/signin" class="btn btn-primary btn-large">Click to sign in</a>
    {{/if}}
</div>
